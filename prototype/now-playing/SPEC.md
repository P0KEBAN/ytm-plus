# Now Playing — Photoshopモック再現仕様

作成・検証: 2026-09-06。Phase 5の単体プロトタイプ。**人間による見た目・動きの承認待ち**。

## 開き方と範囲

**2026-09-07（Phase 6a）から、ローカルの静的サーバー経由で開く。**

```bash
node prototype/now-playing/serve.cjs
# → http://localhost:8080/prototype/now-playing/
```

ビルド、追加ライブラリ、ログインは不要。サーバーは Node だけで動く数十行で、
ループバックにしか bind しない。**`index.html` のダブルクリックでは開けなくなった。**
UI と再生エンジンの境界（Adapter）を `src/js/newui/adapter/` に置き、
プロトタイプがそれを `import` するようにしたためである。ES モジュールは
`file://` では読めない。理由と引き換えに得たものは `docs/ADAPTER-CONTRACT.md` §7。

通常画面の「…」→「プロトタイプの確認」で状態を切り替える。
音声は流れない。時刻、曲長、同期タイミング、翻訳、追加2曲は確認用データ。

- **`manifest.json` は未変更。YouTube Music の DOM/API には接続も変更もしていない。**
  Phase 6a で `src/js/newui/adapter/`（契約とモック実装）を新設したが、
  manifest から読まれないので拡張の動作には一切影響しない。
- 保存、通信、音声再生、拡張への組み込みは行わない。
- 初期画面の曲名・アーティスト表記・7行の歌詞は提供モックのまま。
- `?state=paused` などで状態を直接開ける。`?capture=1` は比較専用で、時計と背景を初期フレームに固定する。
- 全フォルダをまとめて扱うこと。HTMLだけを別の場所へコピーするとCSS/JS/素材が読めない。
- **`assets/artwork.png` は git に入っていない。** 実在のアルバムジャケットで他者の著作物のため、
  public リポジトリには置かない（`.gitignore` 対象）。実体は `private-docs/prototype-assets/artwork.png`。
  復元するには `cp private-docs/prototype-assets/artwork.png prototype/now-playing/assets/`。
  無い場合は自動的に抽象アートへ落ちるので、プロトタイプ自体は動く。

## 1. このUIが必要とする状態

> **【2026-09-07・Phase 6a で置き換え】**
> ここにあった型（`UIState` / `Track` / `LyricLine` / `Palette`）は、
> **プロトタイプが持っていた状態**であって Adapter の契約ではなかった。
> §7 の指摘のとおり、そのまま契約にすると破綻する。
>
> **契約の正本は `src/js/newui/adapter/types.js` に移した。**
> 「なぜその形にしたか」は `docs/ADAPTER-CONTRACT.md`。
> ここに型を書き写すと必ず食い違うので、書かない。

いまの分けかたは次のとおり。

| どこが持つか | 何を持つか |
| --- | --- |
| `PlayerSnapshot` | 現在曲 / 再生状態 / 位置 / 曲長（不明なら null） / 音量 / 消音 / リピート / シャッフル / capabilities / pending |
| `QueueSnapshot` | 取得状態 / **安定IDを持つ**項目 / 現在項目ID |
| `LyricsSnapshot` | **どの曲の歌詞か** / 取得状態 / 行 / 翻訳の独立した取得状態 |
| UIローカル（`player.js` の `uiState`） | 表示切替 / 歌詞の自動追従 / 現在行 / コントラスト / 背景の動き / 直前の非ゼロ音量 / ドラッグ中 |

境界の判断基準は **「YouTube Music を再起動しても復元されるべき値か」**。

とくに効いている点を3つだけ挙げる。詳細は `docs/ADAPTER-CONTRACT.md`。

- **再生位置は購読で配らない。** 離散変化は `subscribe()`、毎フレームの位置は
  `getPosition()`。分けないと歌詞 RAF のために毎フレーム全購読者へ通知が飛ぶ
- **再生状態は真偽値3つではなく単一の列挙。**
  `'idle' | 'playing' | 'buffering' | 'paused' | 'ended'`。
  以前の `mediaPaused` / `playing` / `buffering` は8通りのうち5通りがあり得ない組み合わせだった
- **歌詞スナップショットは自分がどの曲のものかを持つ。** 曲を変えた直後に
  前の曲の歌詞が届く事故を、UI 側で弾けるようにするため

## 2. このUIが発行する操作

> **【2026-09-07・Phase 6a で置き換え】**
> `document` 上の `CustomEvent('player-action')` は**廃止した。**
> 操作の出口は Adapter のメソッドである。一覧は `src/js/newui/adapter/types.js` の
> `PlayerAdapter`。
>
> 変更のうち大きいものは3つ。
>
> - `cycleRepeat` / `toggleShuffle` / `toggleMute` のような**相対操作をやめ、
>   `setRepeat(mode)` / `setShuffle(enabled)` / `setMuted(enabled)` の目標値指定にした。**
>   外部（YouTube Music 本体のUI・キーボード）で値が変わると相対操作は意図とずれる
> - `seekToLyricLine(id)` を廃止。UI 側で行IDを秒へ解決して `seek(sec)` を出す
> - `openQueue` / `closeQueue` / `setHighContrast` / `setMotion` / `resumeLyricFollow` は
>   UIローカル操作なので Adapter を経由しない
>
> **UI は操作の成功を仮定しない。** すべての操作は `Promise<OpResult>` を返し、
> 応答待ちは `pending` に載る。表示は Adapter から返る正規の状態で確定する。

自動検証だけは、`?probe=1` を付けたときに `player.js` が Adapter への操作呼び出しを
`window.__adapterCalls` へ記録する。**検証用の窓であり、UI の制御経路ではない。**

### 操作規則

- ボタンはネイティブbutton。Tabで移動、Space/Enterで実行。
- シークはネイティブrange。矢印で1秒、Home/Endで先頭/末尾。画面の通常位置では左右キーで5秒移動。
- 入力欄・ボタン・設定モーダルの外ではSpaceで再生/停止。入力中のキーは横取りしない。
- 音量は矢印で1、Home/Endで0/100。音量0と消音は同一視しない。
- キューを開くと最初の曲へフォーカス。Tab、Shift+Tab、Enterで選曲。Escapeで歌詞へ戻り、キューの入口へフォーカスを戻す。
- 設定はネイティブdialog。内部にフォーカスを閉じ、Escape/閉じるで「…」へ戻す。
- 手動スクロール中は自動追従を止める。「現在の歌詞に戻る」で再開。
- 現在行は歌詞領域の上から128設計pxに置く。長い行は可変高さで折り返し、行座標を再計測する。
- 曲末尾：ONEは同曲先頭、ALLは次曲へ循環、NONEは一覧の最後で停止。**この判断は Adapter が持つ。**
- ブラウザ非表示中はRAFを止める。**再生位置は Adapter が持っているので、
  描画を止めても時刻はずれない**（以前は `player.js` が時計を持っていたため復帰時にずれた）。

## 3. モックから読み取った数値

PSDのレイヤー境界・文字変換・レイヤー効果から採取。色サンプルとぼかし半径は別記。
基準は添付PNG/PSDの**1920×1080**。数値はCSS上の設計座標で、通常の画面では等倍縮小される。

| 要素 | モックの値 | 再現方法 |
| --- | --- | --- |
| ジャケット | x211 / y113、603×598 | PSDの独立レイヤーを使用。外側角丸約24px |
| ジャケット影 | 黒25%、距離1px、ぼかし30px、広がり0 | `0 1px 30px rgb(0 0 0 / .25)` |
| 曲名 | 描画境界x219 / y748〜789。45px、HiraKakuStdN-W5 | 生テキスト。左端とベースラインをブラウザ比較で補正 |
| アーティスト | 描画境界x219 / y796〜827。35px、HiraKakuStdN-W3 | 生テキスト。長い場合は1行省略、title属性と読み上げには全文 |
| 文字・操作の影 | 黒10%、距離1px、ぼかし15px、広がり0 | text-shadow / drop-shadow。Photoshopとの差は下記 |
| 現在の歌詞 | 描画境界x1084 / y255〜302。48px、HiraKakuStdN-W7 | 生テキスト、ぼかしなし |
| その他の歌詞 | 元の文字サイズ45px、行間隔約128px | CSS blur 3.5px・不透明度88%は目視合わせの推定値 |
| シークバー背景 | x211 / y870、603×15px、角丸7.5px、不透明度約50% | CSS。前景白、背景`rgb(196 196 196 / .5)`は描画合わせ |
| シークバー進捗 | x211 / y871、352×15px、角丸7.5px | 初期位置140/240秒=58.33%。画像の約58.4%に対応 |
| シャッフル | x211 / y940、38×32px | **自作SVG（線画）。PSDのPNGは廃止** |
| 前の曲 | x344 / y934、56×41px | **自作SVG（ベタ塗り＋角丸）** |
| 一時停止 | x481 / y924、60×61px | **自作SVG（角丸バー）。再生時は角丸三角** |
| 次の曲 | x622 / y931、54×40px | **自作SVG（ベタ塗り＋角丸）** |
| リピート | x759 / y936、42×31px | **自作SVG（線画）** |
| その他 | x759 / y758、41×11px | **自作SVG（3つの円）** |
| 右下切替 | x1731 / y992、151×56px、角丸28px | CSS。選択部68×48.42px、角丸24.21pxを基準に再現 |
| 歌詞/キューアイコン | 34×25px / 34×20px | **自作SVG（引用符・リスト）** |

PSD内のジャケットと煙の素材は実際に分離した。UI全体の画像貼り付けではない。
文字はローカルのPostScriptフォント名を`@font-face src: local(...)`で指定する。
通常のCSSの太さ指定だけだと、このMacでは300がW0、400がW4に解決されたためである。
フォント自体の複製・同梱・ダウンロードはしない。

### 操作アイコン（2026-09-06 に差し替え）

**初回実装ではPSDのアイコンレイヤーを1倍のPNGとして書き出して使っていたが、全廃してインラインSVGへ置き換えた。**
PSDのアイコン自体がユーザーが配布サイトから仮に持ってきた素材であり、確定デザインではなかったため。

PNGを捨てた理由は見た目の好みだけではない。

1. 38×32px などの1倍PNGのため、Retinaや拡大時に輪郭がぼける
2. `filter: brightness(0) invert(1)` で白へ潰しているだけなので、状態による色分けができない
3. シャッフル/リピートが細い線画、前後送り/一時停止がベタ塗りで、線の重さが揃っていなかった

差し替え後の規則:

- 全アイコンを `viewBox="0 0 24 24"` の共通グリッドで描く。表示サイズはCSSの `width`/`height` だけで決める
- 主操作（前・再生/一時停止・次）は `.glyph.solid` = ベタ塗り＋`stroke-linejoin: round` で角を丸める
- 副操作（シャッフル・リピート・キュー）は `.glyph.stroked` = 線画、`stroke-width: 2.1`、端点と角は丸
- 色は `fill: currentColor` / `stroke: currentColor`。`.glyph { color: #fff }` の1箇所で決まる
- ボタンの中心座標はモックのまま変えていないため、アイコンの大きさを変えても配置はずれない
- 旧 `.next` / `.repeat` にあった `top: -3px` の微調整は削除した。共通グリッドで光学的に中心が揃うため不要になった

**踏んだ罠**: `hidden` は `HTMLElement` のプロパティで、`SVGElement` には存在しない。
`svg.hidden = true` は単に無関係なJSプロパティを生やすだけで要素は消えず、
**再生アイコンが一生表示されない**状態になっていた。`toggleAttribute('hidden', ...)` へ変更して解決。
念のため CSS 側にも `.glyph[hidden] { display: none }` を置いた。
`<img>` や `<span>` から SVG へ置き換えるときは同じ罠を踏むので注意。

### 色・背景レイヤー（2026-09-06 に方式を作り直した）

**初版はPSD背景を1枚のPNGとして書き出し、3x3の色行列で染め直していた。これは廃止した。**

廃止した理由。色行列は「元の3色を新しい3色へ写す」線形変換なので、
**元と大きく違う色合いのジャケットでは色が破綻する。**
このプロジェクトの視覚上の核は「アートワークの色に画面が染まる」ことなので、
そこが1曲でしか成立しないのでは意味がない。

作り直した根拠は、ユーザーがこの背景を作ったグラデーション生成サイトの出力CSS。
レシピそのものが手に入ったので、画像ではなくレシピを実装した。

```
background-color: #000F1D
radial-gradient(at 20% 26%, #019E9E 0px, transparent 62%)
radial-gradient(at 80% 22%, #2D4B4D 0px, transparent 62%)
radial-gradient(at 72% 80%, #000F1D 0px, transparent 62%)
radial-gradient(at 24% 76%, #AAB580 0px, transparent 62%)
（Duotone / Grain / スケール 77% / ゆがみ 59%）
```

4色はジャケットからスポイトで拾ったもの。**PNGの中央値サンプルより、こちらが正しい元の色。**

現在の実装:

- 4点の放射グラデーションをフラグメントシェーダーで生成する。
  重みは中心から線形に消える形で、CSS の `COLOR 0px, transparent 62%` の**近似**。
  **「CSSと同じ」ではない。** CSS の形状省略時は楕円 + `farthest-corner` なので、
  中心位置と画面比率で62%地点の物理位置が変わる。シェーダーは正規化座標上の固定半径
- 重ね順は CSS と同じ（`background-image` は先頭が最前面）。奥から `spot3` → `spot0` の順に重ねる
- fbm ノイズで座標を歪ませ（`warpAmount` / `warpScale`）、さらに同じノイズで
  明暗を重ねて雲の濃淡を作る（`cloudAmount`）。歪ませるだけだと平坦に見えたため
- 粒は `gl_FragCoord` ベースのハッシュ。画面に固定され、煙と一緒には泳がない
- **スポットの座標は配色ではなく構図。曲が変わっても動かさない。変わるのは色だけ**
- 座標の正本は `smoke.js` の `SPOTS` **ひとつだけ**。シェーダーへは `spotPos0`〜`spotPos3` の
  uniform で、静的版へは `--spot0-x` 等の CSS カスタムプロパティで JS から流し込む。
  （2026-09-07 まで GLSL へ直書きされていて、`SPOTS` を変えても絵が変わらない状態だった）
- 1フレームあたり `fbm` を3回評価する（ゆがみのX/Yで2回、雲で1回）。各 `fbm` は4オクターブ。
  canvas は Retina でも CSS 画素数のままなので4倍描画は避けている。
  **ただし計測しているのはCPUが描画命令を出す時間で、GPUの完了時間ではない。**
  低電力GPUでの実測は Phase 6 の完了条件に入れること

色の供給:

- `mock-data.js` の `palettes` が唯一の供給元。`player.js` がパレットのキーをそのまま
  `--art-<key>` へ流すので、`base` / `spot0`〜`spot3` を足すだけで背景へ届く
- `primary` / `secondary` / `shadow` はUI用（抽象ジャケット、リピート1のバッジ）。背景とは別系統
- 曲が変わると5色すべてを時定数500msで補間する
- **実ジャケットからの色抽出は未実装。** Phase 7 で実装する。今はモックの色を入力とする

WebGL が使えない場合:

- `style.css` の `body` に、同じレシピの静的版を `radial-gradient` で置いてある。
  canvas を透明にするだけでそちらが出るので、JS 側にフォールバック描画のコードは無い
- **座標を変えるときは `smoke.js` の `SPOTS` と `style.css` の body 背景を両方直すこと**

この作り直しで **`assets/smoke-textures.js`（4.7MB のdata URL埋め込み）が不要になり削除した。**
`prototype/` は 5.1MB から 416KB になった。

**未解決**: 明るいジャケットだと背景が明るくなり、白い歌詞とのコントラストが不足する。
低彩度パレットで実際に確認済み。「読みやすさを優先」設定では回避できるが、
既定でも破綻しない仕組み（抽出色の明度を抑える等）が Phase 7 で要る。

### 画面比率

ユーザーの回答：「比率を保ち、余白を調整する」。
`scale = min(viewportWidth / 1920, viewportHeight / 1080)`で、主画面を中央に配置する。
背景は常に全画面、右下切替は画面の右下に固定する。

| ビューポート | scale | 主画面の上下余白 | ジャケット |
| --- | --- | --- | --- |
| 1920×1080 | 1 | 0px | 603×598px |
| 1440×900 | .75 | 45px | 452.25×448.5px |
| 1100×700 | .5729167 | 40.625px | 約345.47×342.60px |

ジャケットの603:598という元レイヤーの比率を保持する。無断で正方形へ変形しない。
1100×700より小さい画面やスマートフォンの新レイアウトは今回の対象外。

## 3.5 調整パネル（tune.js・2026-09-06 追加）

**プロトタイプ専用の調整用UI。拡張本体へは持ち込まない。**

狙いは、見た目の微調整を「AIに言葉で何度も伝え直す」ラリーにしないこと。
背景の質感のように、粒の強さ・細かさ・煙の速さ・彩度の**組み合わせ**で決まるものは、
言葉のやりとりでは収束しない。人間がスライダーで決めた数値を1回渡す方が速い。

使い方:

1. `index.html?tune=1` で開く（または `Shift+T`）
2. スライダーで調整する。値は `localStorage`（キー `ytm-plus.tune`）に自動保存される
3. 「数値を書き出す」で、**既定値から変わった項目だけ**がテキストになる。これをAIへ渡す
4. AIが `smoke.js` の `defaults` と `style.css` の `.view-switch` へ焼き込む

設計上の約束:

- **パネルを開くまで、保存値は一切適用しない。** 通常表示のプロトタイプが調整値で汚れないため
- 発色補正（`saturation` / `brightness` / `contrast`）は既定値のとき演算を飛ばす。
  **ゆがみ・雲・粒は既定値でも常に実行される**（既定が0でないため）
- 調整対象は2系統。背景は WebGL の uniform（`Smoke.setParams`）、右下の切替は CSS カスタムプロパティ
  （`--sw-bg` / `--sw-sel` / `--sw-blur` / `--sw-radius` / `--sw-btn-radius` / `--sw-h` / `--sw-w` / `--sw-pad` / `--sw-tint`）
- 「左右」ボタンでパネルを反対側へ移せる。パネルに隠れた部分を見るため

背景シェーダの uniform と既定値（2026-09-06 に人間がスライダーで決めた値）:

| uniform | 既定 | 意味 |
| --- | --- | --- |
| `spotRadius` | .62 | 各色の広がり。CSS の `transparent 62%` に対応 |
| `fieldScale` | 1.08 | 模様全体の拡大率 |
| `warpAmount` | .24 | ノイズで座標を歪ませる量 |
| `warpScale` | 2.0 | その歪みの細かさ |
| `warpSpeed` | 5 | 動きの速さの倍率 |
| `cloudAmount` | .22 | 雲の濃淡の強さ |
| `grainAmount` | .05 | 粒（ザラザラ）の強さ |
| `noisePixel` | 2 | 粒1つの大きさ(px)。小さいほど細かい |
| `saturation` / `brightness` / `contrast` | 1 / 1 / 1 | 発色 |

右下の切替の既定値も同じ手順で決めた（`--sw-bg` .16 / `--sw-sel` .18 / `--sw-blur` 5px /
`--sw-radius` 36px / `--sw-pad` 5px）。`--sw-radius` の 36px は、`border-radius` が
高さの半分で頭打ちになるため見た目には 28px 相当で、完全な丸を意味する。

## 4. 再現差・制約

- **完全な画素一致ではない。** 同じローカルフォントでもPhotoshopとChromeの文字ラスタライズ、端数座標、影の計算が異なる。
  初期1920px比較で、曲名と現在行の白い字の右端には約2px、アーティストの上下には約1pxの差が残る。
- 非現在行のぼかしはCSSで再実装した。PSDのスマートオブジェクトの最終ラスタとエッジ・濃さが完全には一致しない。
- CSSの影はPSDの数値を引き継いだが、Photoshopの「サイズ」とCSSのぼかし半径は同じ描画式ではない。
- 動く背景の形は静止モックだけでは指定されていない。**初期フレームもPSDの煙そのものではない。**
  `capture=1` は時刻を0に止めるだけで、ゆがみ・雲・粒は初期状態でも適用される。
  現在の背景は配色レシピからの再生成であり、元画像の保持ではない。
- 元モックには停止アイコン、ローディング、キュー、翻訳、長い曲名、設定画面がない。これらは下記の仮決め。
- フォントがインストールされていないMacではHiragino Sansへフォールバックし、幅や太さが変わる。
- WebGLが利用できない場合は `style.css` の `body` に置いた静的版が出る。同じ配色・同じ重ね順だが
  **同一ではなく近似**。CSSの`radial-gradient`は既定で楕円+farthest-cornerなので、シェーダーの
  正規化座標上の固定半径とは62%地点の物理位置が違う。ノイズ・雲・拡大を切って比較して
  平均3.82/255・最大17/255の差（2026-09-07 実測）。
- 本番接続時の非同期操作失敗、シーク応答待ち、独立した翻訳取得中/翻訳エラー、曲長不明、キューの取得失敗は未実装。
  現在の「歌詞取得エラー」や「読み込み中」を、それらの対応済みという意味に広げないこと。

### コントラスト

初期表示はモックの明るさと白文字を優先する。そのため、明るい左側の白文字はすべての場所で
4.5:1を満たすわけではない。既定のぼかした歌詞も、読みやすさのための設定ではない。

「…」→「読みやすさを優先」で、`#001018`の52%オーバーレイを背景に足し、全歌詞のぼかしと透過を解除する。
右下の切替も暗い背景へ変更する。フォーカスは白3px＋濃紺の外周で、明暗の背景双方で認識できる。
1440×900で前景だけを隠し、実際のブラウザ背景と白のコントラストを全画素で計算した。
下記は初期フレームの最小値。文字のアンチエイリアスやぼかしを含む実効可読性の測定ではない。

**測定は `check.cjs` が毎回やり直し、`contrast.json` へ書き出す**（2026-09-07 に復活させた。
それ以前は古い測定結果が出力フォルダに残っていただけで、実行しても更新されていなかった）。

背景を作り直した後の実測値（1440×900、前景を隠して全画素）:

| 配色 | 背景全体の最小 | 歌詞領域（右56%）の最小 |
| --- | --- | --- |
| original | 2.91:1 | 5.58:1 |
| warm | 3.36:1 | 6.94:1 |
| neutral | 2.95:1 | 6.02:1 |

**読み取り方を間違えないこと。** 歌詞領域は3配色とも 4.5:1 を満たしている。
4.5:1 を割っているのは**左側（曲名・アーティスト・再生操作）**で、これは
**Photoshopモックの時点からそうだった**（元モックの背景全体最小は 2.09:1）。
つまり既定でコントラストが低いのは事故ではなく、ユーザーが選んだ絵である。

未解決なのは別の話で、**本当に明るいジャケット（白系・パステル）が来たとき**に
歌詞領域まで割り込む可能性がある。モックの `neutral` はそこまで明るくないので未証明。
`contrast.json` が毎回出るようになったので、実データを流せば数値で分かる。

対処案（Phase 7）。**背景全体を一律に暗くする案は採らない。** ユーザーが承認した絵が壊れるため。

1. 抽出色を OKLCH 等で扱い、色相を保ったまま明度に上限をかける
2. 曲情報・操作の下と歌詞領域に、薄い固定グラデーションの膜を敷く（局所的に効かせる）
3. 最終色の相対輝度クランプは保険として最後に置く。白文字で 4.5:1 を確保する上限は約 `0.183`

完全なWCAG適合やスクリーンリーダー実機受入は未宣言。

## 5. 仮決めした点と理由

| 判断 | 仮決め・理由 |
| --- | --- |
| Apple Musicに近い構成 | 今回のユーザー説明を優先。旧文書の「模倣しない」を理由にモックを変えない |
| 常設要素 | モックどおり。「…」に音量・翻訳・読みやすさ・動き・試作状態確認をまとめた |
| 進捗 | モックに時刻表示がないため140/240秒とした。時刻はバーのhover/focus時だけ補助表示 |
| 原文と翻訳 | 原文48/45pxを保ち、訳を24pxで下に配置。翻訳ON時は末尾48設計pxをフェードさせて画面外の行の切断を和らげる。訳は確認用で正式な楽曲訳ではない |
| 長い曲名・アーティスト | 1行省略で再生制御の位置を保つ。全文はtitle属性、読み上げ、キューで参照できる |
| 長い歌詞 | 複数行に折り返し、曲名のように省略しない |
| キュー | 右側の歌詞領域を置き換える。新しい常設ボタンは増やさない |
| 初期リピート/シャッフル | NONE / OFF。ON状態は小さな点、ONEは数字を追加 |
| 追加曲の画像 | 提供されていないため、指定配色を使う抽象的な代替画像 |
| 自動追従 | 現在行を上寄りに保つ。行選択または復帰ボタンで再開 |
| 背景の動き | 約57秒/74秒のゆるい変形。最大変位は各軸約2.8%。速度の好みは人間レビュー対象 |
| 色の遷移 | 時定数500msの補間。約1.5秒で95%遷移。OSの動き低減では即時 |
| モックへの追加状態 | 選定済みデザインを使った最小限の試作。製品仕様の承認済み扱いにはしない |

## 6. 検証と性能

実際のChromeでファイルを直接開き、PNGと横並びにして自分で比較した。

**検証は自動化してある。`check.cjs` を実行すること（2026-09-06 に private-docs から
リポジトリ内へ移した。失うと作り直しが高くつくため）。**

```bash
YTM_PLAYWRIGHT=<playwrightのパス> node prototype/now-playing/check.cjs
```

playwright はこのプロジェクトの依存ではないので、場所を環境変数で渡す。
出力先の既定は `private-docs/phase5-verification/`（git 追跡外）。`YTM_OUT` で変えられる。
`checks.json` が操作検証の結果、`contrast.json` がコントラスト測定、
残りは各状態・各画面幅のスクリーンショット。

**見た目を変えたら必ずこれを通すこと。** ただし**これだけでは足りない**。
アイコンをSVG化したとき、`checks.json` の「pause button changes」は通っていたのに
再生アイコンが一度も表示されない不具合が残っていた。あの検証は `aria-label` の変化を
見ていて、実際の描画を見ていなかったためである。**必ずスクリーンショットを人間かAIの目で見ること。**

- 1920×1080で配置・背景を比較し、1440×900、1100×700でも撮影。
- 通常再生、停止、再生ローディング、歌詞取得中、歌詞なし、歌詞エラー、翻訳ON、長文、低彩度を表示して確認。
- 再生キー、3状態リピート、整数音量、シーク、消音、キュー選曲とフォーカス復帰、歌詞クリック、手動スクロール復帰、再試行を確認。
- `prefers-reduced-motion`で背景が静止することと、通常時に背景が変化することを、前景を除いたスクリーンショットで確認。
- 外部HTTP(S)リクエスト0、ブラウザJavaScriptエラー0。
- 補足検証：翻訳末尾のフェード、WebGL無効時の静止背景と色変更も成功。
- 既存の構文チェックとテスト：**145 pass / 7 skip / 0 fail**（2026-09-07 時点）。
  内訳は既存 106 + Adapter 契約テスト 39。

**Phase 6a の書き換え後、書き換え前と画素で比較して退行が無いことを確認した（2026-09-07）。**
`feeb06a` を worktree に取り出して同じ条件で撮り、全画面を1画素ずつ比べた。

| 比較 | 結果 |
| --- | --- |
| 9状態 × 1440×900（遷移が終わるまで待って撮影） | **全て完全一致** |
| `check.cjs` が撮る13枚 | `1440-loading.png` 以外は**完全一致** |
| キュー画面・設定ダイアログ・キュー選曲直後・2曲目の歌詞 | **全て完全一致** |

`1440-loading.png` だけ 0.038%（最大 122/255）差が出るが、これは `#spinner` の
`animation: spin 1s linear infinite` の回転位相で、**本質的に非決定**である。

この比較の過程で `check.cjs` の待ち時間の不足が3箇所見つかったので直した。
現在行の `filter/opacity` は .35s、`.icon:active` の `transform` は .15s、
翻訳は Adapter が遅れて届ける。**待たずに撮ると実行ごとに絵が変わって比較できない。**

歌詞のRAFはキャッシュされた時刻配列を二分探索する。行の切替時だけclassを変更する。
DOM全体の再構築は曲変更と翻訳切替時だけ。行座標は行変更・翻訳・ResizeObserverによる変更時だけ読む。
バーのtransformは最大10Hz、時刻テキストとrange値は値が変わった時だけ更新する。
背景は同じRAFから最大30fpsでGPUへ描画命令を出す。RetinaでもキャンバスはCSS画素数に抑える。

計測はChromeヘッドレス上のJSコールバック時間であり、GPU実行時間や実ブラウザの総描画負荷ではない。
初回計測で同じ時刻文字列を100msごとに書き直す不要なレイアウトが見つかり、値が変わる場合だけの更新へ修正した。
最終計測（Chrome 152.0.7977.76）：145フレーム、平均0.048ms、p95 0.20ms、最大0.30ms。
別の1.5秒区間のレイアウトは4回、合計0.629ms。毎フレームのレイアウトではなく時刻表示等の変更に伴うもの。
測定値はこの環境のスナップショットで、他環境の性能保証ではない。詳細は`checks.json`。

人間が確認する項目：背景の速度、ノイズの強さ、文字の微細な差、歌詞追従の感触、追加状態の見た目。
このレビューが終わるまではPhase 5完了やAdapter接続開始としない。

## 7. Phase 6 へ渡す前に直すこと（2026-09-07 レビューの結果）

> **【2026-09-07・Phase 6a で全8件に対処した】**
> 契約は `src/js/newui/adapter/types.js`、判断の理由は `docs/ADAPTER-CONTRACT.md`。
> `tests/adapter-contract.test.mjs` が 39 件でこれを機械的に守っている。
> **以下は「何が問題だったか」の記録として残す。**

Codex に別の目でレビューさせた結果、**このファイルの §1・§2 をそのまま Adapter 契約として
使うと破綻する**ことが分かった。ここは「プロトタイプUIが持っている状態」であって、
「Adapter が満たすべき契約」ではない。Phase 6 の最初の作業は、両者を分けることである。

指摘された不足（そのまま Phase 6 の設計課題とする）:

| # | 問題 | なぜ効くか |
| --- | --- | --- |
| 1 | `Track[]` が状態型のどこにもないのに、キューを `trackIndex` と `Track[]` から導出すると書いてある | 型として不完全。実装できない |
| 2 | `trackIndex` はUIを配列の並び順に結合している | 実キューは挿入・削除・並べ替えがある。**安定したキュー項目IDが要る** |
| 3 | `Track` が全曲分の歌詞を持っている | 現在曲の歌詞を遅延取得する実装と噛み合わない |
| 4 | `mediaPaused` / `playing` / `buffering` の3真偽値 | 矛盾した組み合わせを表現できてしまう。実際に描画と時計が別の値を見ている |
| 5 | 曲長不明、シーク不可、キュー/翻訳の取得中・失敗、操作の応答待ち・失敗が無い | 本番では全部起きる |
| 6 | `toggleShuffle` / `cycleRepeat` は現在値からの相対操作 | 外部（YTM側）で値が変わると意図とずれる。**`setShuffle(enabled)` / `setRepeat(mode)` の目標値指定にする** |
| 7 | `activeLine` / `follow` / `view` / `highContrast` / `motion` / `lastNonzeroVolume` が同じ型に混ざっている | これらはUIローカル状態でAdapter由来ではない |
| 8 | `seekToLyricLine(id)` が Adapter をUIの歌詞行IDに結合している | UI側で秒へ解決して `seek` を出すほうが境界が明確 |

分けかたの方針:

```text
PlayerSnapshot   現在曲 / 再生状態 / 位置 / 曲長(nullable) / 音量 / リピート / シャッフル / capabilities
QueueSnapshot    取得状態 / 安定IDを持つ項目 / 現在項目ID
LyricsSnapshot   歌詞の取得状態 / 行 / 翻訳の独立した取得状態
UIローカル       表示切替 / 追従 / コントラスト / 動き / 直前の非ゼロ音量
Adapter操作      setRepeat(mode) / setShuffle(enabled) / setMuted(enabled) など明示的な目標値
```

**操作後にUIが成功を仮定しないこと。** Adapter から返る正規の状態で確定させる。

この表は Phase 6 の入力である。**Phase 5 のプロトタイプ自体の不具合ではない**ので、
プロトタイプの承認をこれで止める必要はない。

### 対処（2026-09-07・Phase 6a）

| # | 対処 |
| --- | --- |
| 1 | `Track[]` を廃止。`QueueSnapshot` が自分で項目を持つ |
| 2 | `trackIndex` を廃止し、`itemId`（キュー項目）と `videoId`（曲）を分けた |
| 3 | 歌詞を曲から切り離し、`LyricsSnapshot` として遅延取得する |
| 4 | 真偽値3つを `'idle'\|'playing'\|'buffering'\|'paused'\|'ended'` の単一列挙にした |
| 5 | 曲長 `null`、`seekable`、`QueueStatus` / `LyricsStatus` / `TranslationStatus`、`pending`、`OpResult` を型に入れた |
| 6 | `setRepeat(mode)` / `setShuffle(enabled)` / `setMuted(enabled)` の目標値指定にした |
| 7 | UIローカル状態を `player.js` の `uiState` へ分離した |
| 8 | `seekToLyricLine` を廃止。UI が行IDを秒へ解決して `seek(sec)` を出す |

**契約に入れなかったが実装で判明した、もっとも効いた設計判断**は
「再生位置を購読で配らない」ことである（`subscribe()` と `getPosition()` の分離）。
これが無いと歌詞 RAF のために毎フレーム全購読者へ通知が飛ぶ。
`docs/ADAPTER-CONTRACT.md` §3.2。
